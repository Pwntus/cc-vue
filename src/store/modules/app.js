import Vue from 'vue'
import * as type from '../mutation-types'

const state = {
	
}

const mutations = {
	
}

const actions = {
	
}

const getters = {
	
}

/* Export module */
export default {
	state,
	mutations,
	actions,
	getters
}
